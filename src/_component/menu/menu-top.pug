each goTo in data.site.content.contentKeyList 
  input.menu__controller(
    type="radio"
    id=`super-${goTo}`
    name="work-stroke-work"
    checked=goTo.checked
  )
  label.menu__top( for=`super-${goTo}`)
    h4=`${goTo}`
